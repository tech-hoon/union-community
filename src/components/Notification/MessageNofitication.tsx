import React from 'react';
import { NotificationType } from 'types';

interface Props {
  notification: NotificationType;
}

const MessageNofitication = ({ notification }: Props) => {
  return <div></div>;
};

export default MessageNofitication;
