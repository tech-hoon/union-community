import { Messenger as MessengerIcon } from '@styled-icons/remix-line';

interface Props {}

const Messenger = (props: Props) => {
  return (
    <>
      <MessengerIcon size='30' />
    </>
  );
};

export default Messenger;
